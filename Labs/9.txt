/*Дана последовательность вещественных чисел и количество членов в последовательности. Вычислить сумму отрицательных чисел в последовательности. */

#include<stdio.h>
#include<math.h>
#include <locale.h>

int main()
{
setlocale (LC_ALL, "Rus");
int k, sum=0, i;
printf("Введите количество членов в последовательности:\n");
scanf("%d", &k);
int mas[k];
printf("Введите последовательность:\n");
for (i=0; i<k; i++)
scanf("%d", &mas[i]);
for (i=0; i<k; i++)
if (mas[i]<0)
sum += mas[i];
printf("Сумма отрицательных чисел в последовательности = %d", sum);
_getch();
return 0;
}
